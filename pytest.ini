[pytest]
testpaths = tests
# Repo-Wurzel und tests/ auf den Importpfad — sonst finden die Tests weder
# die Hub-Module (ui, vault, oauth) noch die conftest-Hilfen.
pythonpath = . tests
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
